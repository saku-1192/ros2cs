*** TYPE_SUPPORT_COMMON_HPP_.orig	2025-07-13 04:00:00.000000000 +0900
--- TYPE_SUPPORT_COMMON_HPP_.mod	2025-07-13 04:00:00.000000000 +0900
***************
*** 24,36 ****
-inline std::string
-_create_type_name(
-std::string message_namespace,
-std::string message_name)
-{
-std::ostringstream ss;
-if (!message_namespace.empty()) {
-    ss << message_namespace << "::";
-  }
-  ss << "dds_::" << message_name << "_";
-return ss.str();
-}
-
-inline std::string
-_create_type_name(
-const message_type_support_callbacks_t * members)
-{
-if (!members) {
-RMW_SET_ERROR_MSG("members handle is null");
-return "";
-  }
-std::string message_namespace(members->message_namespace_);
-std::string message_name(members->message_name_);
-return _create_type_name(message_namespace, message_name);
-}
+// [MODIFIED FOR ros2cs-jazzy]
+inline std::string
+_create_type_name(
+  std::string message_namespace,
+  std::string message_name)
+{
+  std::string ret = "";
+  if (!message_namespace.empty()) {
+    ret += message_namespace + "::";
+  }
+  ret += "dds_::" + message_name + "_";
+  return ret;
+}
+
+// [MODIFIED FOR ros2cs-jazzy]
+inline std::string
+_create_type_name(
+  const message_type_support_callbacks_t * members)
+{
+  if (!members) {
+    RMW_SET_ERROR_MSG("members handle is null");
+    return "";
+  }
+  std::string message_namespace(members->message_namespace_);
+  std::string message_name(members->message_name_);
+  return _create_type_name(message_namespace, message_name);
+}
